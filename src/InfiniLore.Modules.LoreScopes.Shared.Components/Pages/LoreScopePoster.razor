@* ------------------------------------------------------------------------------------------------------------------ *@
@* Imports
@* ------------------------------------------------------------------------------------------------------------------ *@

@* ------------------------------------------------------------------------------------------------------------------ *@
@* Descriptors
@* ------------------------------------------------------------------------------------------------------------------ *@

@* ------------------------------------------------------------------------------------------------------------------ *@
@* Html
@* ------------------------------------------------------------------------------------------------------------------ *@
<div class="@Class w-40 h-60 rounded-md overflow-hidden relative">
    @if (ImageUrl is not null) {
        <img src="@ImageUrl"
             alt="LoreScope: @Name"
             class="w-full h-full object-cover rounded-md"/>
    }
    else {
        <div class="w-full h-full bg-(--color-base-95) flex items-center justify-center">
            <span class="text-xs text-(--color-base-80)">
                <LiFileImage class="w-24 h-24"/>
            </span>
        </div>
    }
</div>
@* ------------------------------------------------------------------------------------------------------------------ *@
@* Code
@* ------------------------------------------------------------------------------------------------------------------ *@
@code {
    [Parameter, EditorRequired] public string? ImageUrl { get; set; }
    [Parameter, EditorRequired] public string? Name { get; set; }

    [Parameter] public string Class { get; set; } = string.Empty;
}